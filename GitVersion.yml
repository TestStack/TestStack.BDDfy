mode: ContinuousDeployment
assembly-versioning-scheme: MajorMinorPatch
branches:
  main:
    regex: ^main$
    is-release-branch: true
    increment: Patch

  other:
    regex: .*
    increment: Patch
ignore:
  sha: []